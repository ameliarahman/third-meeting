function fullName(name) {
    var objek ={
        name :  name,
    } ;

    console.log(objek);
}

function nim(nim) {
    var objek ={
        nim:  nim,
    } ;

    console.log(objek);
}

function user(hobby) {
    var objek ={
        hobby:  hobby,
    } ;

    console.log(objek);
}

module.exports = {
   fullName,
   nim,
   user
  };
