function greet() {
    console.log("Hello!");
}

const name = "Kembang Prima Rossari";

function calculateSum(x, y) {
    return x + y;
}

function multiply(a, b) {
    return a * b;
}

module.exports = {
    greet,
    name,
    calculateSum,
    multiply,
};